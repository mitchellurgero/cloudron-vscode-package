/**
 * Read bytes from a Uint8Array.
 */
export declare class Reader {
    private readonly array;
    private _offset;
    private currentClamp;
    private textDecoder;
    constructor(array: Uint8Array);
    get offset(): number;
    skip(amount: number): this;
    clamp(): void;
    unclamp(): void;
    jump(offset: number): this;
    eof(): boolean;
    peek(amount: number): Uint8Array;
    peek(amount: number, encoding: "utf8"): string;
    read(amount: number): Uint8Array;
    read(amount: number, encoding: "utf8"): string;
}
