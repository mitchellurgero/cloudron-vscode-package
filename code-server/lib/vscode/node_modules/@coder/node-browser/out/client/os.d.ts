/// <reference types="node" />
import * as os from "os";
import { NotImplementedProxy } from "../common/proxy";
import * as Message from "../common/messages";
declare type NodeOs = typeof os;
export declare class OsModule extends NotImplementedProxy implements Partial<NodeOs> {
    private data;
    constructor(initData: Promise<Message.Server.InitData>);
    homedir(): string;
    readonly EOL: string;
}
export {};
